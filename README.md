# VisionTrack-AI
A real-time face recognition attendance system built using Python, Flask, and OpenCV. It utilizes the LBPH (Local Binary Patterns Histograms) algorithm for biometric identification and logs attendance automatically to a CSV-based dashboard.
